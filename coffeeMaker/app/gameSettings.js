(function () {

    var setting = {
        windowWidth: 30*32,
        windowHeight: 19*32
    }

    window.setting = setting;

    // более короткие имена объектам
    window.o = window.object;
    window.s = window.sprite;
})();